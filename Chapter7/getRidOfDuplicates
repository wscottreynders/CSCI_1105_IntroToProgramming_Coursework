package exercise07_15;

import java.util.Scanner;

public class arrrayWithoutDuplicates {
	private static Scanner input;
	private static int[] numbers;
	
	public static void main(String[] args) {
		input = new Scanner(System.in);
		numbers = new int[10];
		
		System.out.print("Enter 10 numbers separated by a space: ");
		for (int i = 0; i < numbers.length; i++ ) {
			numbers[i] = input.nextInt();
		}
			
			System.out.print("The number of distinct numbers is ");
			
			System.out.print("Array without duplicates: " + eliminateDuplicates(numbers));
		
		
	}

	public static int[] eliminateDuplicates(int[] list) {
		int eliminateDuplicates = list[0];
		int[]  uniqueNumbers = new int [10];
			for (int i = 0; i < numbers.length; i++ ) {
				if (i != i - numbers.length )
			}
		}
	}
	
}
